<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="<%= root_path %>">Bucketlist del 18º</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor01">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <a class="nav-link" href="<%= root_path %>">Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= done_tasks_path %>">Completadas</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Incompletas</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= new_task_path %>">Añadir nueva tarea</a>
      </li>
    </ul>
    <ul class="navbar-nav my-2 my-lg-0">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <% if user_signed_in? %>
          <%= current_user.email %>
        <% else %>
          Ingreso
        <% end %>
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <% if user_signed_in? %>
            <a class="dropdown-item" href="<%= edit_user_registration_path %>">Editar Usuario</a>
            <a class="dropdown-item" href="<%= destroy_user_session_path %>" data-method="DELETE">Cerrar Sesión</a>
          <% else %>
            <a class="dropdown-item" href="<%= new_user_registration_path %>">Crear Usuario</a>
            <a class="dropdown-item" href="<%= new_user_session_path %>">Iniciar Sesión</a>
          <% end %>
        </div>
      </li>
    </ul>
  </div>
</nav>